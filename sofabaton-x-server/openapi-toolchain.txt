# The exact versions the committed openapi.json was generated with.
# CI installs these before the drift test; regenerate the document with
# the same set (pip install -r sofabaton-x-server/openapi-toolchain.txt)
# so framework-version wording never shows up as API drift.
fastapi==0.141.1
starlette==1.6.0
pydantic==2.13.5
pydantic-core==2.46.5
uvicorn==0.52.4
httpx==0.28.1
